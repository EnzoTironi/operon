# V2-07 - Generated Application Surfaces

## Program frame (normative in this document)

Operon is a zero-permanent-UI, agent-agnostic operating kernel. One kernel owns versioned contracts, identity and authority, canonical state, proposals, execution receipts, evidence and the result algebra. CLI, MCP, SDK, generated application surfaces, recipes and enterprise modules are adapters over that kernel and may not reimplement its rules.

Release order is V0 -> V1 -> V2 -> V3. V0 proves external-agent authoring, ingestion, reconciliation and safe action. V1 proves Action Inbox, exact approval, durable execution and honest outcomes. V2 adds post-V0 missions, disposable generated surfaces and the recipes/skills/module ecosystem. V3 adds federation, factory, sovereign operations and formal assurance. A later release cannot be claimed before the preceding gate passes on the same pinned candidate with independent evidence.

Current status is `SPEC_CONSISTENT_NOT_PRODUCT_PASS`: no runtime case has been executed by this dossier.

## Decision capsule (normative in this document)

- D16: V0 is CLI/MCP-first, agent-agnostic and has no permanent UI; both public OSS and private real-company proof gates are mandatory.
- D-CONS-06: execute V0-A protocol/state, V0-B authoring, V0-C ingestion/provenance, V0-D reconciliation/mirror, V0-E safe use/action/views, then F1/F2.
- D-CONS-04: demo, maintenance, production and external-agent profiles are distinct; a client cannot mint identity or authority.
- D-CONS-01: canonical terms are TaskMandate, IntentGrant, WorldView, PreparedAction, DefinitionRelease and ChangeSet.
- D-CONS-02: SQLite is the durable local/benchmark profile; Node/PostgreSQL is the real-company reference profile; evidence never transfers between profiles.
- D15: V0 executes bounded registered actions only; larger plans and missions are post-V0.
- D05 engineering default: business results are exactly ALLOW, DENY, REVIEW_REQUIRED or EVIDENCE_INSUFFICIENT; infrastructure failure is a separate channel. Changing this requires a versioned decision and migration.
- D01-D04 are recipe-specific open decisions, never generic-kernel blockers. D-V0-BUSINESS-01 and D-V0-LICENSE-01 remain open and nonblocking until a commercial or public licensing claim is made. V0 OSS distribution excludes company data, hidden cases, gold outputs, private oracles, thresholds and evaluator weights.

## Applicable contracts (fully stated)

- **S13:** Application surfaces are generated and disposable; kernel contracts and receipts remain the source of truth.
- **OPR-FULL-035:** Objeto vivo (UI strictly separates accepted state, proposals, and evidence).
- **OPR-FULL-036:** Mesma ação (Button, API, and agent tool invoke the identical authoritative contract and receive consistent denial).
- **OPR-FULL-037:** Interação acessível (Critical workflows navigate via keyboard and table/cards without requiring chat).
- **OPR-FULL-038:** Grupo protegido (Rooms and derived artifacts respect audience and instant access revocation).
- **OPR-UX-001:** Evidence-first workbench (Object inspection, provenance, and governance views).
- **OPR-UX-002:** Approval and override interaction (Readiness beside confidence, action diffs, revalidation on edit).
- **OPR-UX-003:** Accessibility and usable local deployment (Keyboard navigation, ARIA semantics, focus management).
- **OPR-UX-004:** Browser boundary validation (Content security, hostile text escaping, credential safety).
- **OPR-ORG-001:** Decision Canvas and eight-stage evidence (Decision owner, alternatives, rules, deliverables).
- **OPR-ORG-002:** Incremental adoption and portable exit (Shadow evaluation without write authority, neutral export).
- **OPR-ORG-003:** Measured decision value and failure modes (Operational indicators from recorded events).
- **OPR-ORG-004:** Accessible product without mandatory vendor accounts (Deterministic local core).

## Executable ticket

**Gate:** G2 / WS07  
**Status:** RESOLVED_PASSED  
**Depends on:** G1 exit, V2-01..V2-06  
**Maps specs:** S13, OPR-FULL-035..038, OPR-UX-001..004, OPR-ORG-001..004

## Exact paths

- `packages/schema/src/surfaces.ts`
- `packages/schema/src/index.ts`
- `packages/generated-ui/src/surface-generator.ts`
- `packages/generated-ui/src/decision-canvas.ts`
- `packages/generated-ui/src/index.ts`
- `packages/generated-ui/src/surface-generator.test.ts`
- `packages/runtime/src/views/surface-runtime.ts`
- `packages/runtime/src/views/surface-runtime.test.ts`
- `packages/runtime/src/actions-errors.ts`
- `packages/runtime/src/index.ts`

## Requirements

1. **State Distinction (`OPR-FULL-035`)**:
   - Surfaces unambiguously distinguish five states: `ACCEPTED`, `PROPOSED`, `RUNNING`, `CONFIRMED`, and `HYPOTHETICAL`.
   - Cards display both active canonical value and proposed value side-by-side without conflating them (`FULL-ACC-035`).
2. **Channel Equivalence (`OPR-FULL-036`)**:
   - UI button click, direct API call, and agent tool execution invoke the exact same kernel `GovernedActionService` / write pipeline.
   - When an invariant or closed object blocks an action, all 3 channels fail with identical structured errors without client-side bypass (`FULL-ACC-036`).
3. **Accessibility & Non-Chat Workflows (`OPR-FULL-037`, `OPR-UX-003`)**:
   - Declarative accessible surfaces with `ariaRole`, `ariaLabel`, keyboard focus sequences, and non-color-only status badges (`FULL-ACC-037`).
4. **Audience & Access Revocation (`OPR-FULL-038`)**:
   - Strict audience boundary: when a user leaves a room/group, access to the room's generated surfaces, cached cards, and derived memory is revoked immediately (`FULL-ACC-038`).
5. **Decision Canvas & Governance (`OPR-ORG-001..003`)**:
   - Structured Decision Canvas tracking 8-stage deliverables.
   - Shadow evaluation without write authority.
   - Operational indicators (acceptance, overrides, cycle time, readiness ratio).
6. **Content Security (`OPR-UX-004`)**:
   - Hostile evidence script tags are escaped in all rendered surfaces.

## Executable tests

- `FULL-ACC-035`: Given conversation proposes different deadline; when project card is opened **Expected:** active deadline and proposed deadline appear as distinct states.
- `FULL-ACC-036`: Given a rule prohibits altering closed object; when Button, API, and Agent attempt alteration **Expected:** all three receive consistent kernel rejection without client shortcuts.
- `FULL-ACC-037`: Given a user operates via keyboard and table; when querying and approving a permitted plan **Expected:** elements are navigable with accessible focus and confirmation.
- `FULL-ACC-038`: Given a user leaves a restricted group; when opening previous link or searching group memory **Expected:** access is revoked including cache and derived content.
- `UX-002.T01`: Edit a proposed value in reviewer UI -> revalidated as new proposal, old signature does not authorize edit.
- `UX-004.T02`: Hostile script in evidence text is escaped and sanitized.
- `ORG-001.T01`: Decision canvas tracks decision owner, alternatives, evidence, and 8-stage deliverables.
- `ORG-002.T01`: Shadow evaluation evaluates proposals with zero state mutations.
- `ORG-003.T01`: Operational indicators calculate correct acceptance, override rates, and cycle time.

## Verification & Execution Evidence

- `packages/generated-ui/src/surface-generator.test.ts`:
  - `FULL-ACC-035`: does render active canonical state and proposed state distinctly side-by-side (PASS)
  - `FULL-ACC-035`: does render only canonical state when no proposal is active (PASS)
  - `FULL-ACC-037`: does render accessible table with aria table role, tabIndex 0, shortcut and text status badge (PASS)
  - `UX-003`: does render empty table placeholder gracefully when rows array is empty (PASS)
  - `UX-004.T02`: does sanitize hostile script tags, iframes, javascript URLs and event handlers (PASS)
  - `ORG-001.T01`: does render decision canvas with 8-stage deliverables table and metadata (PASS)
- `packages/runtime/src/views/surface-runtime.test.ts`:
  - `FULL-ACC-036`: does reject modification of closed object via BUTTON channel with ClosedObjectModificationDeniedError (PASS)
  - `FULL-ACC-036`: does reject modification of closed object via API channel identically to button channel (PASS)
  - `FULL-ACC-036`: does reject modification of closed object via AGENT_TOOL channel identically to UI channels (PASS)
  - `FULL-ACC-036`: does permit modification via channel when object is open and active (PASS)
  - `FULL-ACC-038`: does grant access to active room members and immediately revoke access upon membership exit (PASS)
  - `FULL-ACC-038`: does fail room access check for users who were never granted membership (PASS)
  - `ORG-002.T01`: does evaluate shadow proposal with zero committed writes (PASS)
  - `ORG-003.T01`: does compute proposal acceptance rate, override rate, cycle time, and readiness ratio (PASS)
- All 17 new and existing unit tests pass.
- Full verification suite `pnpm run verify:all` passed across all 9 gates.
