# Archive

Documents in this directory are historical annexes of the V0 to V3 dossier. They are kept as written and are not a contract for the current product. Nothing in `packages/` is required to satisfy them.

Content was moved, not edited. Relative paths inside these files still refer to their original locations.

## Index

| Path | Was | Status |
| :-- | :-- | :-- |
| `tickets/OPR-*.md` (353 files) | `docs/tickets/` | Fragmented V1 to V3 backlog. All `OPEN`. Not a backlog to execute. |
| `tickets/V0-CH-*.md` (12 files) | `docs/tickets/` | V0 critical path tickets. Status text still says `OPEN_NOT_IMPLEMENTED`; closing or deleting them is a founder decision. |
| `tickets/V1-*.md`, `V2-*.md`, `V3-*.md` (24 files) | `docs/tickets/` | Release tickets marked `RESOLVED_PASSED` by the gap analyses below, without independent evidence. |
| `gap-analyses/V0-GAP-ANALYSIS.md` to `V3-GAP-ANALYSIS.md` | `docs/` | In-process self-assessment. Gate statuses in these files are not a product pass. |
| `EFFECT-AUDIT-REPORT.md` | `docs/` | Point-in-time Effect and type-system audit dated 2026-09-10. Snapshot, not a roadmap. |
| `pilot/RUNBOOK.md` | `pilot/runbook/RUNBOOK.md` | F2 consented real-company mirror runbook. No participant exists; the F1/F2 program is frozen for phase 1. |

## Not archived

`docs/specs/` and `docs/adrs/` stay where they are. The license ADR is being closed in a separate branch (`cursor/close-license-adr-b442`) that touches those directories; moving them would conflict with it. Archiving the specs and ADRs is deferred until that branch lands.
