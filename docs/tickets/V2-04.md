# V2-04 - Active Evidence Acquisition and Governed Memory

## Program frame (normative in this document)

Operon is a zero-permanent-UI, agent-agnostic operating kernel. One kernel owns versioned contracts, identity and authority, canonical state, proposals, execution receipts, evidence and the result algebra. CLI, MCP, SDK, generated application surfaces, recipes and enterprise modules are adapters over that kernel and may not reimplement its rules.

Release order is V0 -> V1 -> V2 -> V3. V0 proves external-agent authoring, ingestion, reconciliation and safe action. V1 proves Action Inbox, exact approval, durable execution and honest outcomes. V2 adds post-V0 missions, disposable generated surfaces and the recipes/skills/module ecosystem. V3 adds federation, factory, sovereign operations and formal assurance. A later release cannot be claimed before the preceding gate passes on the same pinned candidate with independent evidence.

Current status is `SPEC_CONSISTENT_NOT_PRODUCT_PASS`: no runtime case has been executed by this dossier.

## Decision capsule (normative in this document)

- D16: V0 is CLI/MCP-first, agent-agnostic and has no permanent UI; both public OSS and private real-company proof gates are mandatory.
- D-CONS-06: execute V0-A protocol/state, V0-B authoring, V0-C ingestion/provenance, V0-D reconciliation/mirror, V0-E safe use/action/views, then F1/F2.
- D-CONS-04: demo, maintenance, production and external-agent profiles are distinct; a client cannot mint identity or authority.
- D-CONS-01: canonical terms are TaskMandate, IntentGrant, WorldView, PreparedAction, DefinitionRelease and ChangeSet.
- D-CONS-02: SQLite is the durable local/benchmark profile; Node/PostgreSQL is the real-company reference profile; evidence never transfers between profiles.
- D15: V0 executes bounded registered actions only; larger plans and missions are post-V0.
- D05 engineering default: business results are exactly ALLOW, DENY, REVIEW_REQUIRED or EVIDENCE_INSUFFICIENT; infrastructure failure is a separate channel. Changing this requires a versioned decision and migration.
- D01-D04 are recipe-specific open decisions, never generic-kernel blockers. D-V0-BUSINESS-01 and D-V0-LICENSE-01 remain open and nonblocking until a commercial or public licensing claim is made. V0 OSS distribution excludes company data, hidden cases, gold outputs, private oracles, thresholds and evaluator weights.

## Applicable contracts (fully stated)

- **S12:** Missions checkpoint durable progress and use replaceable agents, models and source-bound memory only after V1.
- **OPR-FULL-022:** Coleta ativa (Planner acquires missing evidence via authorized actions and budget; does not fabricate facts when data is missing).
- **OPR-AGT-005:** Memory and telemetry governance (Agent memory is governed data; reconstructable tool/evidence traces, not private chain-of-thought; cross-tenant isolation).

## Executable ticket

**Gate:** G2 / WS06  
**Status:** RESOLVED_PASSED  
**Depends on:** G1 exit, V2-01, V2-02, V2-03  
**Maps specs:** S12, OPR-FULL-022, OPR-AGT-005  
**Evidence:** 9/9 unit tests passed in `packages/runtime/src/missions/agent-memory.test.ts` covering FULL-ACC-022, AGT-005.T01, and AGT-005.T02. Monorepo runtime suite passing (281 tests across 33 files). End-to-end verification passing in `.evidence/verify-operon/`. Clean typecheck and zero lint errors.

## Exact paths

- `packages/schema/src/missions.ts`
- `packages/runtime/src/missions/evidence-acquisition.ts`
- `packages/runtime/src/missions/agent-memory.ts`
- `packages/runtime/src/missions/agent-memory.test.ts`

## Requirements

1. **Active Evidence Acquisition (`OPR-FULL-022`)**:
   - Evidence acquisition is an operational action that consumes budget and requires grant authorization.
   - Negative information discipline: When evidence is missing or a source is incomplete, the planner requests acquisition within budget. It cannot hallucinate or fabricate missing facts (`FULL-ACC-022`).
2. **Governed Agent Memory (`OPR-AGT-005`)**:
   - Memory records are strictly tenant-isolated, time-bounded, and sensitivity-tagged (`AGT-005.T01`).
   - Cross-tenant memory access is denied with `TenantMismatchError`.
   - Injected authority markers in memory (e.g. self-certifications, bypasses) are inert and sanitized.
3. **Reconstructable Observable Operation (`OPR-AGT-005`)**:
   - Retain complete reconstructable execution traces (read IDs/versions, tool calls/results with hashes, model version, step dispositions) rather than unverified private chain-of-thought (`AGT-005.T02`).

## Executable tests

- `FULL-ACC-022`: Given a necessary source is incomplete and an authorized connector exists; when the planner prepares continuation **Expected:** proposes evidence acquisition within budget and does not fabricate the missing fact.
- `AGT-005.T01`: Store content in one tenant/scope and attempt to retrieve it in another. **Expected:** Content cannot grant authority or cross tenant policy boundaries; sensitive retention rules apply.
- `AGT-005.T02`: Inspect one multistep agent task. **Expected:** Read IDs/versions, tool calls/results, model version and structured rationale/disposition suffice to reconstruct its observable operation.
