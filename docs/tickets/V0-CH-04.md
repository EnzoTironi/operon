# V0-CH-04 - Versioned skills and recipes as public product artifacts

## Program frame (normative in this document)

Operon is a zero-permanent-UI, agent-agnostic operating kernel. One kernel owns versioned contracts, identity and authority, canonical state, proposals, execution receipts, evidence and the result algebra. CLI, MCP, SDK, generated application surfaces, recipes and enterprise modules are adapters over that kernel and may not reimplement its rules.

Release order is V0 -> V1 -> V2 -> V3. V0 proves external-agent authoring, ingestion, reconciliation and safe action. V1 proves Action Inbox, exact approval, durable execution and honest outcomes. V2 adds post-V0 missions, disposable generated surfaces and the recipes/skills/module ecosystem. V3 adds federation, factory, sovereign operations and formal assurance. A later release cannot be claimed before the preceding gate passes on the same pinned candidate with independent evidence.

Current status is `SPEC_CONSISTENT_NOT_PRODUCT_PASS`: no runtime case has been executed by this dossier.

## Decision capsule (normative in this document)

- D16: V0 is CLI/MCP-first, agent-agnostic and has no permanent UI; both public OSS and private real-company proof gates are mandatory.
- D-CONS-06: execute V0-A protocol/state, V0-B authoring, V0-C ingestion/provenance, V0-D reconciliation/mirror, V0-E safe use/action/views, then F1/F2.
- D-CONS-04: demo, maintenance, production and external-agent profiles are distinct; a client cannot mint identity or authority.
- D-CONS-01: canonical terms are TaskMandate, IntentGrant, WorldView, PreparedAction, DefinitionRelease and ChangeSet.
- D-CONS-02: SQLite is the durable local/benchmark profile; Node/PostgreSQL is the real-company reference profile; evidence never transfers between profiles.
- D15: V0 executes bounded registered actions only; larger plans and missions are post-V0.
- D05 engineering default: business results are exactly ALLOW, DENY, REVIEW_REQUIRED or EVIDENCE_INSUFFICIENT; infrastructure failure is a separate channel. Changing this requires a versioned decision and migration.
- D01-D04 are recipe-specific open decisions, never generic-kernel blockers. D-V0-BUSINESS-01 and D-V0-LICENSE-01 remain open and nonblocking until a commercial or public licensing claim is made. V0 OSS distribution excludes company data, hidden cases, gold outputs, private oracles, thresholds and evaluator weights.

## Applicable contracts (fully stated)

- **S14: Recipes and skills are versioned product artifacts over public kernel contracts.**
- **S18: Agent-facing discovery and invocation are protocol-equivalent.**

## Executable ticket

**Gate:** V0-C  
**Status:** OPEN_NOT_IMPLEMENTED  
**Depends on:** V0-CH-02

## Open forks — decide before coding

- [ ] Package signature/trust root format
- [ ] Registry distribution protocol

If an open fork changes a public schema, state machine, security boundary or migration, stop and record the decision. Do not choose silently.

## Objective

Implement this bounded V0 slice through shared domain services. Passing this issue closes only its mapped V0 child, not its constitutional parent.

## Exact paths

- `packages/skills/`
- `packages/recipes/`
- `packages/mcp/src/resources/`
- `packages/cli/src/commands/`

New paths must stay inside the nearest package above unless the architecture decision explicitly creates a shared package.

## Environment contract

- `OPERON_PROFILE=local|production|external-agent`
- `OPERON_DATABASE_URL=<sqlite path or postgres URL>`
- `OPERON_TENANT_ID=<server-bound tenant>`
- `OPERON_ENVIRONMENT_ID=<server-bound environment>`
- `OPERON_AUTH_ISSUER=<qualified issuer URL>`
- `OPERON_MCP_TRANSPORT=stdio|http`
- `OPERON_RESULT_VERSION=operon.result/v1`
- `OPERON_EVIDENCE_DIR=<run-owned evidence path>`

Secrets are supplied by the runtime/secret store, never committed or printed.

## Code sketch

```ts
type SkillManifest = {
  id: string;
  version: string;
  minContract: string;
  requiredTools: string[];
  authorityPrerequisites: string[];
  inputSchema: object;
  outputSchema: object;
  digest: string;
};
```

The sketch fixes semantics, not final names. CLI and MCP must call the same service implementation.

## Steps

1. Add or freeze request/result schemas and typed errors before implementation.
2. Write failing positive, negative, unauthorized and recovery tests.
3. Implement the service behavior in the runtime package.
4. Add thin CLI and MCP adapters with no bypass logic.
5. Add restart/concurrency/fault-injection coverage where state changes.
6. Record migration and rollback behavior.
7. Run the pinned suite and capture candidate-bound evidence. Keep unavailable cases `NOT_RUN`.

## Tests

- [ ] generic agent discovers and follows skill
- [ ] missing/incompatible tool blocks before mutation
- [ ] recipe import grants no authority

- [ ] Wrong tenant/environment/resource reference does not disclose existence.
- [ ] Logs do not corrupt machine-readable results.
- [ ] The same idempotency key with different input conflicts.

## Binary acceptance

- [ ] Every listed test passes on the exact candidate/profile.
- [ ] CLI and MCP outcomes are semantically equivalent where both apply.
- [ ] No client field creates identity, authority or successful business outcome.
- [ ] Restart and retry preserve one honest state.
- [ ] Evidence includes command, exit, stdout/stderr, state receipt, candidate/tree/lock/profile digests and independent verifier result.
- [ ] Parent ticket remains open unless all of its independent obligations pass.

## Rollback

1. Disable the new operation/contract version in discovery.
2. Stop accepting new writes while preserving readable receipts and old records.
3. Revert adapter routing, not historical evidence.
4. Run backward/forward migration checks against a copy of persisted state.
5. Document unsupported in-flight states and reconciliation steps; never delete unknown outcomes.

## Non-goals

- No permanent Studio/Workspace.
- No native mission runtime or first-party hosted agent.
- No hidden enterprise/federation dependency.
- No production credentials, real-company action or protected benchmark material created by this issue.

## Self-containment rule

The program frame, decisions, applicable contracts, paths, environment, tests, binary acceptance and rollback are all stated here. IDs are audit lineage only; no external file is needed to execute or judge this ticket.
