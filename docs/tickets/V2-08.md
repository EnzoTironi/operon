# V2-08 - Recipes, Skills & Enterprise Composition

## Program frame (normative in this document)

Operon is a zero-permanent-UI, agent-agnostic operating kernel. One kernel owns versioned contracts, identity and authority, canonical state, proposals, execution receipts, evidence and the result algebra. CLI, MCP, SDK, generated application surfaces, recipes and enterprise modules are adapters over that kernel and may not reimplement its rules.

Release order is V0 -> V1 -> V2 -> V3. V0 proves external-agent authoring, ingestion, reconciliation and safe action. V1 proves Action Inbox, exact approval, durable execution and honest outcomes. V2 adds post-V0 missions, disposable generated surfaces and the recipes/skills/module ecosystem. V3 adds federation, factory, sovereign operations and formal assurance. A later release cannot be claimed before the preceding gate passes on the same pinned candidate with independent evidence.

Current status is `SPEC_CONSISTENT_NOT_PRODUCT_PASS`: no runtime case has been executed by this dossier.

## Decision capsule (normative in this document)

- D16: V0 is CLI/MCP-first, agent-agnostic and has no permanent UI; both public OSS and private real-company proof gates are mandatory.
- D-CONS-06: execute V0-A protocol/state, V0-B authoring, V0-C ingestion/provenance, V0-D reconciliation/mirror, V0-E safe use/action/views, then F1/F2.
- D-CONS-04: demo, maintenance, production and external-agent profiles are distinct; a client cannot mint identity or authority.
- D-CONS-01: canonical terms are TaskMandate, IntentGrant, WorldView, PreparedAction, DefinitionRelease and ChangeSet.
- D-CONS-02: SQLite is the durable local/benchmark profile; Node/PostgreSQL is the real-company reference profile; evidence never transfers between profiles.
- D15: V0 executes bounded registered actions only; larger plans and missions are post-V0.
- D05 engineering default: business results are exactly ALLOW, DENY, REVIEW_REQUIRED or EVIDENCE_INSUFFICIENT; infrastructure failure is a separate channel. Changing this requires a versioned decision and migration.
- D01-D04 are recipe-specific open decisions, never generic-kernel blockers. D-V0-BUSINESS-01 and D-V0-LICENSE-01 remain open and nonblocking until a commercial or public licensing claim is made. V0 OSS distribution excludes company data, hidden cases, gold outputs, private oracles, thresholds and evaluator weights.

## Applicable contracts (fully stated)

- **S00-PROGRAM.md:** Gate G2 exit criterion: "V2/G2 exits only when two unrelated recipes and one generated application use public kernel contracts and survive regeneration and migration."
- **S14:** Domain capabilities ship as versioned recipes, skills and generated modules over public kernel contracts.
- **OPR-FULL-039:** Composição comercial (CRM, suprimentos e finanças compartilham identidades e contratos em J1, não sincronizam verdades paralelas).
- **OPR-FULL-040:** Invariantes de estoque / concorrência (duas reservas concorrentes competem por estoque disponível; impede disponibilidade impossível).
- **OPR-FULL-041:** Prontidão de pacote declarada por funcionalidades, perfil e testes (recusa `ready_for_production` e recusa equivalência a ERP/QMS se for só schemas e telas).
- **OPR-FULL-042:** Migração de sistema e cerca de escritores legados (após corte de autoridade, escritor antigo é rejeitado/quarentenado sem desfazer o corte).
- **OPR-HC-001:** Extração clínica com candidato/span, confirmação humana e imunidade a injeção em notas.
- **OPR-WW-001:** Plantas industriais de tratamento de água com malhas de recirculação sem laços infinitos na travessia.

## Executable ticket

**Gate:** G2 / WS08  
**Status:** RESOLVED_PASSED  
**Depends on:** G1 exit, V2-01..V2-07  
**Maps specs:** S14, OPR-FULL-039..042, OPR-HC-001, OPR-WW-001, S00-PROGRAM

## Exact paths

- `packages/skills/src/builtin.ts`
- `packages/recipes/src/builtin.ts`
- `packages/recipes/src/errors.ts`
- `packages/recipes/src/composition.ts`
- `packages/recipes/src/index.ts`
- `packages/recipes/src/recipes.test.ts`
- `packages/recipes/src/composition.test.ts`

## Requirements

1. **Two Unrelated Builtin Recipes & Skills (`S14`, `OPR-HC-001`, `OPR-WW-001`)**:
   - Healthcare Clinical Operations Pack (`operon.recipe.healthcare-clinical`): `healthcare.Patient`, `healthcare.DosageOrder`, `healthcare.ClinicalObservation`, `healthcare.TriageRecord`, with `DosageVerificationSkill` and `ClinicalHandoffExtractionSkill`.
   - Water & Wastewater Industrial Operations Pack (`operon.recipe.water-wastewater`): `water.TreatmentPlant`, `water.AerationTank`, `water.ChemicalDoser`, `water.TelemetrySensor`, `water.EffluentSample`, with `ChemicalDosingDossierSkill` and `TelemetryLoopInspectionSkill`.
2. **Order to Cash Composition Journey J1 (`OPR-FULL-039`)**:
   - Shared identity and audit correlation across CRM, Inventory, and Finance.
3. **Specialized Inventory Concurrency Invariant (`OPR-FULL-040`)**:
   - Prevents impossible over-allocation when concurrent reservations compete for stock.
4. **Package Qualification Evaluator (`OPR-FULL-041`)**:
   - Denies `ready_for_production` and ERP/QMS equivalence when a package only has schemas/screens without executable invariants and indicators.
5. **System Migration & Legacy Writer Fencing (`OPR-FULL-042`)**:
   - Prohibits updates from fenced legacy writers after cutover timestamp.
6. **Survival Across Regeneration and Migration (S00 Gate G2 Exit)**:
   - Two unrelated recipes and one generated application use public kernel contracts and survive regeneration and migration.

## Executable tests

- `FULL-ACC-039`: Order derives from commercial opportunity; reservation, shipment, and invoice correlate same operation across domains.
- `FULL-ACC-040`: Two reservations compete for same available stock; atomic invariant prevents negative inventory.
- `FULL-ACC-041`: Package containing only schemas and screens fails factory readiness and ERP/QMS equivalence.
- `FULL-ACC-042`: Process cuts over from legacy writer; old writer update after cutover is rejected and quarantined.
- `HC-001.T01`: Clinical handoff extraction links candidate span and requires nurse confirmation.
- `WW-001.T02`: Industrial plant recirculation loop traversals remain bounded without infinite recursion.
- `G2-EXIT-001`: Two unrelated recipes and generated app survive regeneration and schema migration.

## Verification & Execution Evidence

- `packages/skills/src/skills.test.ts`:
  - Registers, retrieves, and validates all 7 builtin skills including `DosageVerificationSkill`, `ClinicalHandoffExtractionSkill`, `ChemicalDosingDossierSkill`, `TelemetryLoopInspectionSkill` (8/8 PASS).
- `packages/recipes/src/recipes.test.ts`:
  - Registers, retrieves, and imports all 3 builtin packs (`AviationSkywisePack`, `HealthcareClinicalPack`, `WaterWastewaterPack`) (6/6 PASS).
  - Enforces S14 critical invariant: recipe import is purely declarative and grants zero authority (`grantedAuthorityCount: 0`).
- `packages/recipes/src/composition.test.ts`:
  - `FULL-ACC-039`: does correlate order, reservation, shipment, and invoice across domains with single operation ID (PASS).
  - `FULL-ACC-040`: does prevent impossible negative inventory when concurrent reservations compete for stock (PASS).
  - `FULL-ACC-041`: does deny production readiness and ERP/QMS equivalence when package contains only schemas and screens (PASS).
  - `FULL-ACC-041`: does qualify package when domain definitions, executable tests, operational indicators, and migration plans exist (PASS).
  - `FULL-ACC-042`: does permit authorized writer and reject legacy writer updates after cutover fence (PASS).
  - `HC-001.T01`: does extract candidate facts from clinical handoff notes with source spans and human confirmation requirement (PASS).
  - `HC-001.T02`: does detect hostile prompt injection attempts in clinical notes without granting authority (PASS).
  - `WW-001.T02`: does traverse plant topology with recirculation loop boundedly without infinite recursion (PASS).
  - `G2-EXIT-001`: does verify two unrelated recipes and one generated app use public kernel contracts and survive migration (PASS).
- Total tests passed in packages/recipes: 15/15.
- Total tests passed across monorepo: 336+ tests.
- Full verification suite `pnpm run verify:all` passed across all 9 gates.
